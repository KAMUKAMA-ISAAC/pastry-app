# URL of the backend API (no trailing slash) — required.
# The backend is deployed separately on Render; point this at it (or at
# http://localhost:8000 for local dev against a local backend).
REACT_APP_BACKEND_URL=https://pastry-app-ohtm.onrender.com
